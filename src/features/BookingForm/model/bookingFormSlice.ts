import { createSlice } from "@reduxjs/toolkit";
const initialState = {};

const bookingFormSlice = createSlice({
  name: "bookingFormSlice",
  initialState,
  reducers: {},
});

export const { actions, reducer } = bookingFormSlice;
