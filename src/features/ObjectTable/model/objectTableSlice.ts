import { createSlice } from "@reduxjs/toolkit";

const objectTableSlice = createSlice({
  name: "objectTableSlice",
  initialState: {},
  reducers: {},
});

export const { actions: objectTableActions, reducer: objectTableReducer } =
  objectTableSlice;
